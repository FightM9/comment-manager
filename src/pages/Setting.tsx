import CommentSort from '../modules/CommentSort';
import { Card } from '../style/Card';

const Setting = () => {
  return (
    <main>
      <Card>
        <CommentSort />
      </Card>
    </main>
  );
};

export default Setting;
